Chào bạn, một dự án rất thú vị và thiết thực! Dựa trên cấu trúc thư mục và các file bạn đã cung cấp (`Web_ComputerVision`, `AIUD_Module1_6-main`, các file Markdown trắc nghiệm, và file Word phân bổ tài nguyên), tôi xin phác thảo bản thiết kế chi tiết để nâng cấp trang web môn học **Computer Vision** của bạn.

Mục tiêu là biến các thư mục rời rạc hiện tại thành một hệ thống **LMS (Learning Management System) tĩnh** (Static Site) mạch lạc, tích hợp trình chấm trắc nghiệm và quản lý Notebook.

---

### BẢN THIẾT KẾ CHI TIẾT WEBSITE: COMPUTER VISION COURSE

#### 1. Cấu trúc Hệ thống (Site Architecture)

Chúng ta sẽ giữ nguyên cấu trúc file để tận dụng code cũ, nhưng sẽ quy hoạch lại luồng điều hướng (Navigation Flow).

**Sơ đồ luồng người dùng (User Flow):**
`Trang chủ`  `Chọn Chương`  `Giao diện học tập (Tabbed Interface)`:

1. **Tab Lý thuyết:** Xem Slide/PDF (Dùng template `pdf_viewer`).
2. **Tab Thực hành:** Danh sách Bài tập & Google Colab Notebooks.
3. **Tab Trắc nghiệm:** Hệ thống Quiz (Module `AIUD`).

**Cấu trúc thư mục đề xuất (Cập nhật):**

```text
Web_ComputerVision/
├── index.html                  # Trang chủ (Dashboard môn học)
├── assets/                     # Chứa css, js, images dùng chung
│   ├── css/style.css
│   └── js/main.js
├── AIUD_Module_Quiz/           # (Đổi tên từ AIUD_Module1_6-main) - Engine trắc nghiệm
│   ├── index.html
│   ├── script.js
│   └── DB/                     # Chứa các file CSV câu hỏi (Converted)
├── Chuong_1/                   # Thư mục từng chương
│   ├── index.html              # Trang chính của chương
│   ├── Tai_lieu.pdf            # Slide bài giảng
│   └── data.json               # (Mới) Config cho bài tập/Notebook chương này
├── Chuong_2/ ...
└── ...

```

---

#### 2. Thiết kế chi tiết từng Phân hệ

##### A. Phân hệ Bài tập & Google Colab (Mới)

Dựa trên file `Phân bổ tài nguyên học Thị giác Máy tính.docx`, chúng ta sẽ thiết kế giao diện danh sách bài tập. Thay vì hard-code vào HTML, chúng ta nên tạo một cấu trúc hiển thị rõ ràng.

**Giao diện đề xuất:** Sử dụng **Card** hoặc **Table** có nút "Open in Colab".

**Mã HTML mẫu (Chèn vào `index.html` của từng chương):**

```html
<div class="notebook-section">
    <h3>📂 Bài tập & Thực hành Google Colab</h3>
    <table class="notebook-table">
        <thead>
            <tr>
                <th>Tuần/Chủ đề</th>
                <th>Tên Notebook</th>
                <th>Mức độ</th>
                <th>Thao tác</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>Tuần 1</td>
                <td>Computer Vision Course Overview</td>
                <td><span class="badge easy">Cơ bản</span></td>
                <td>
                    <a href="LINK_GOOGLE_COLAB" target="_blank" class="btn-colab">
                        <img src="https://colab.research.google.com/assets/colab-badge.svg" alt="Open In Colab">
                    </a>
                </td>
            </tr>
            <tr>
                <td>Bài tập 1</td>
                <td>Chuyển đổi định dạng ảnh (PNG sang JPG)</td>
                <td><span class="badge medium">Thực hành</span></td>
                <td>
                    <a href="#" class="btn-download">⬇ Tải Source</a>
                </td>
            </tr>
        </tbody>
    </table>
</div>

```

##### B. Phân hệ Trắc nghiệm (Nâng cấp từ `AIUD`)

Hiện tại thư mục `AIUD_Module1_6-main` đang chạy Quiz dựa trên file CSV trong thư mục `DB`. Bạn có các file `Trắc nghiệm T1-3.md`, cần chuyển đổi chúng sang định dạng CSV mà `script.js` của hệ thống này hiểu được.

**Quy trình chuyển đổi (Markdown -> CSV):**
Các file Markdown của bạn thường có dạng:

```markdown
Câu 1: Câu hỏi...
A. Đáp án A
B. Đáp án B
C. Đáp án C
D. Đáp án D
Đáp án: A

```

Hệ thống `AIUD` (file `script.js` và các file `MD*.csv`) thường yêu cầu cấu trúc CSV như sau:
`ID, Question, OptionA, OptionB, OptionC, OptionD, Answer, Explanation (Optional)`

**Giải pháp tự động hóa:**
Tôi sẽ viết cho bạn một script Python nhỏ để bạn chạy, nó sẽ tự động đọc file `Trắc nghiệm T1.md` và sinh ra file `MD_Chuong1.csv` chuẩn format cho web.

**(Script Python chuyển đổi - Bạn có thể chạy trên máy):**

```python
import csv
import re

def convert_md_to_csv(md_file_path, csv_output_path):
    with open(md_file_path, 'r', encoding='utf-8') as f:
        content = f.read()

    # Regex để bắt các thành phần (Tuỳ chỉnh theo format thực tế trong file MD của bạn)
    # Giả sử format: "Câu 1: ... \n A. ... \n B. ... \n Đáp án: ..."
    pattern = re.compile(r'Câu \d+:(.*?)(?:A\.(.*?))?(?:B\.(.*?))?(?:C\.(.*?))?(?:D\.(.*?))?Đáp án:\s*([A-D])', re.DOTALL)
    
    matches = pattern.findall(content)
    
    with open(csv_output_path, 'w', newline='', encoding='utf-8-sig') as csvfile:
        writer = csv.writer(csvfile)
        # Header chuẩn cho script.js của AIUD module
        writer.writerow(['ID', 'Question', 'A', 'B', 'C', 'D', 'Answer'])
        
        for idx, match in enumerate(matches):
            q_text = match[0].strip()
            op_a = match[1].strip() if match[1] else ""
            op_b = match[2].strip() if match[2] else ""
            op_c = match[3].strip() if match[3] else ""
            op_d = match[4].strip() if match[4] else ""
            ans = match[5].strip()
            
            writer.writerow([idx + 1, q_text, op_a, op_b, op_c, op_d, ans])

# Gọi hàm convert
# convert_md_to_csv('Web_ComputerVision/Chương 1.../Trắc nghiệm T1.md', 'Web_ComputerVision/AIUD.../DB/MD1_New.csv')

```

**Tích hợp vào Web:**
Trong file `index.html` của từng Chương, bạn sẽ thêm một nút hoặc iframe trỏ về module Quiz với tham số:
`<a href="../AIUD_Module_Quiz/index.html?module=MD1_New.csv" class="btn-quiz">Làm Trắc Nghiệm Chương 1</a>`
*(Cần sửa nhẹ `script.js` trong AIUD để nhận tham số URL `?module=...` thay vì hardcode file CSV)*.

---

#### 3. Kế hoạch triển khai (Action Plan)

**Bước 1: Chuẩn hóa dữ liệu**

1. Dùng Script Python trên để convert toàn bộ `Trắc nghiệm T1-3.md` thành `MD_Ch1.csv`, `MD_Ch2.csv`...
2. Đưa các file CSV này vào thư mục `AIUD_Module1_6-main/DB/`.

**Bước 2: Xây dựng Trang chi tiết Chương (Template)**
Tạo một file mẫu `template_chapter.html` để nhân bản cho 8 chương. File này cần có:

* **Header:** Tên chương (Ví dụ: "Chương 1: Tổng quan...").
* **Nav Tabs:** [Lý thuyết] | [Bài tập & Code] | [Trắc nghiệm].
* **Content:**
* Tab 1: Iframe chứa PDF Viewer (đã có sẵn trong code cũ).
* Tab 2: Table chứa danh sách Notebook (copy từ file Word phân bổ).
* Tab 3: Iframe hoặc Link dẫn tới trang Quiz.



**Bước 3: Cập nhật Trang chủ (`index.html` gốc)**

* Biến nó thành Dashboard đẹp mắt.
* Danh sách 8 chương trình bày dạng **Grid Card**. Mỗi Card hiển thị: Tên chương, Số lượng bài tập, Trạng thái (Đã học/Chưa học - lưu bằng LocalStorage đơn giản).

**Bước 4: Nâng cấp `script.js` của Quiz App**
Sửa file `Web_ComputerVision/AIUD_Module1_6-main/script.js`  để nó đọc được file CSV động.

* Hiện tại: Có thể nó đang load cố định `MD1.csv`.
* Sửa đổi:
```javascript
// Lấy tên file csv từ URL, ví dụ: index.html?id=MD1
const urlParams = new URLSearchParams(window.location.search);
const csvFile = urlParams.get('id') || 'MD1'; // Mặc định MD1 nếu không có tham số

// Sau đó dùng biến csvFile để load dữ liệu
d3.csv(`DB/${csvFile}.csv`).then(function(data) { ... });

```



### Bạn có muốn tôi cung cấp đoạn code đầy đủ cho file `template_chapter.html` (kết hợp cả slide, notebook và nút trắc nghiệm) để bạn dùng thử cho Chương 1 không?